namespace ITI.GRPCLAB.Server.Models
{
    // Enum for Category should match the proto definition
    public enum Category
    {
        Food = 0,
        Electronics = 1,
        Furniture = 2
    }
}
